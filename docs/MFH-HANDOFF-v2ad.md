# MFH 핸드오프 v2ad

> 이전 상태: `MFH-HANDOFF-v2ac.md` (일지 사진 다중화) 참조.
> **이번 세션: 선교편지 5-에이전트 팀 구성 → 기조("보고+기도")·온두라스 웹검색·letter참조 확립 → 5월 리허설 검증.**

---

## 현재 위치 (한 줄)

선교편지 **5-에이전트 팀 + "보고+기도" 기조**가 확립됨. 온두라스 WebSearch(중립)·앱 `letter` 인사이트 참조까지 반영, 5월 데이터로 전 과정 리허설·검증 완료. 새 리허설(처음부터)·실제 6월호 준비.

---

## 이번 세션 완료

### 1. 팀 구성 (5종 + 인프라)
- `.claude/agents/letter-{collector,strategist,writer,designer,assembler}.md` — 각 §9 8항목 틀.
- 운영 마스터 `docs/MFH-LETTER-AGENTS.md` (헌법: 아키텍처·핸드오프 계약·다양성·라우팅·공통규칙·런북).
- `CLAUDE.md §7·§9` 팀 포인터 → 새 세션 자동 인지.
- 팀장(Claude)=오케스트레이터, 매 단계 우진 승인. 서브에이전트 직접 대화 없음(전부 팀장 경유).

### 2. 자동 수집 스크립트 V2 (`scripts/fetch-letter-materials.mjs`)
- photos jsonb **다중사진**(최대 5장) + **인사이트**(★편지적용 `in_letter`) + **중보연계**(`intercession_id`).

### 3. 편집 기조 "보고+기도" (strategist·writer) ★핵심
- 스토리텔링(산만) ↔ 보고형(딱딱) 조합: **도입은 이야기 / 사역은 항목별 [보고+🙏기도] / 마지막 기도제목 요약 반복**.
- 구조: 표지 → 도입 → [온두라스] → [사역] 항목①②③… → [가정] → 기도제목 요약 → 마무리. (이벤트 많으면 사역 면 증가)
- 다양성은 **도입 메시지·제목 2~3안**에 집중(본문 항목식 고정).

### 4. 온두라스 = WebSearch 기반 (strategist)
- 그달 기상·경제·사회·치안 검색 → **중립 승화**(정당·인물·진영 거명 금지). `tools: WebSearch` 추가.
- 근거: 기존 반자동 프로세스(PROJECT-INSTRUCTIONS/KNOWLEDGE).

### 5. 앱 `letter` 인사이트(편지 초안 추천) 참조
- insights `letter` 도메인 = 앱이 만든 편지 방향·3단 초안 추천. collector 수집·표시, strategist **출발점**으로 참조 후 그달 자료·웹검색으로 검증·보완.

### 6. 5월 리허설 검증 (`letter-templates/issues/2026-05/`, gitignore)
- collector→strategist(WebSearch)→writer 전 과정 정상. 새 기조 본문 품질 확인(문장 교정은 실제 호 작성 때 실무 보정).
- 디자인 이슈 해결(letter.html, 로컬): 로고 경로(`file://` 상위경로 문제 → issue폴더로 복사), 사역카드 본문 겹침, 기도제목 **뱃지·폰트 확대**, 마무리 카드 재배치, 후원계좌(우리은행 1002-349-524757 김우진) 반영.
- `mfh-cardnews.html` 마스터에 **기도제목 그룹 뱃지** 반영.

---

## 다음 할 일

1. **새 방법으로 처음부터 별도 리허설** (우진 예정) — "○월호 만들어줘".
2. **실제 6월호** — 6월 일지 쌓인 뒤. 본문 문장 교정은 그때 실무 보정.
3. (보류) **designer/마스터를 새 기조로 재구성** — 항목 카드(소제목·날짜+사진+본문+🙏) + 기도제목 요약면. 새 리허설 때.
4. (선택) PDF 자동화(puppeteer 미설치), 사역 도면 크롭 보강.

---

## 주의사항

- 서브에이전트 = **세션 아니라 파일 정의**. 같은 repo 새 세션이면 자동 인식. 단계 간 정보는 **핸드오프 파일**(`issues/<월>/`)로만 연결(에이전트는 기억 안 함).
- `issues/<월>/`·호별 인스턴스 html = **gitignore**(개인자료). 마스터 템플릿·정의서·docs는 추적.
- 디자인은 **새 리허설 때 처음부터** (5월 letter.html은 옛 스토리텔링 구조, 로컬에만 존재).
- 비용: 월 1회 수동. WebSearch는 온두라스 1~2회로 제한. (memory `api-cost-sensitive`)
- 디자인 선호: 파스텔·차분 (memory `design-tone-preference`).

---

## 관련 커밋 (push 완료)
- `85760c2` 팀 구성(5종 + 마스터 + 스크립트 V2 + CLAUDE 포인터 + 핸드오프)
- `872a6f1` 기조 개선(보고+기도 · 온두라스 WebSearch · letter 인사이트 참조 · 기도제목 뱃지)
- *(이 핸드오프 갱신은 다음 커밋)*

*작성: 2026-06 세션.*
