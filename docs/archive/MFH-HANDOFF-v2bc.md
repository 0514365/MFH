# MFH 핸드오프 v2bc (세션 종료)

> 이전: `v2bb`(앱 전반 점검 + 군살 정리, 동작 변화 0). 이번 세션: **인사이트 본문 마크다운 표시 개편** — 카드 본문이 raw `**`/`【】`를 그대로 노출하던 것을, 무의존 포매터로 정리(볼드·라벨·재정렬·날짜). **3 commit 모두 push 완료.**

---

## 현재 위치 (한 줄)

**인사이트 본문 표시 포매터 완성·배포.** 표시 단계 처리라 기존 저장분에도 즉시 반영. 다음 세션은 칩 색·간격 등 실기기 피드백 미세조정 또는 잔여 백로그 자유 선택.

---

## 이번 세션 작업 (3 commit)

| 단계 | 내용 | commit |
|------|------|--------|
| 1 | **`app/insights/InsightContent.tsx` 신설** — 인사이트 본문 표시 포매터(마크다운 라이브러리 없이 자체 구현). 공통: `**…**`→볼드(기호 제거)·`【…부…】`→볼드. 기도제목 라벨(사역/가정/나라) → `나라`=`온두라스` 재라벨 + `온두라스→사역→가정` 재정렬(prayer=소프트 칩 `bg-primary-soft`+본문 아래 줄, 그 외 도메인=인라인 볼드 라벨). fruit=앞머리 날짜 볼드+내용 다음 줄. **렌더 3곳(InsightsClient `InsightCard`·saved `SavedClient`·`DomainInsightPanel`)에서 raw `whitespace-pre-wrap` → `<InsightContent>` 교체.** 프롬프트(`lib/insightPrompt.ts`) Prayer·Letter 기도제목을 처음부터 `온두라스→사역→가정` 순으로 생성하도록 수정 | `f0610c0` |
| 2 | "기도제목" 헤딩도 볼드(제목 정규식에 `/^기도제목:?$/` 추가) | `8a7310c` |
| 3 | `【2부…】` 타이틀이 `【1부…】`와 같이 볼드되도록 — 실제 생성문은 `【2부…】 — 부제`처럼 같은 줄에 부제가 붙어 줄끝 `】` 앵커(`/^【.*】$/`)에 안 걸렸음 → `/^【[^】]*】/`로 변경(부제 뒤따라도 매칭) | `04c08ba` |

## 핵심 설계 메모 (다음 세션 참고)

- **표시만 바꾸고 내용은 안 바꾼다** → 기존 저장분도 새로고침하면 즉시 적용. 프롬프트 변경은 *새* 생성분의 순서·라벨만 맞춘다(이중 안전).
- 기도제목 라벨 묶음: 연속된 `"[-] 라벨 · 본문"` 줄을 수집 → 재라벨·재정렬 후 도메인별 렌더. 구분자 `·`=U+00B7(프롬프트와 동일 코드포인트 확인). 라벨 직후 `·`/`:` 구분자 필수라 본문 속 `·`(예: `광고·통역`)·fruit 날짜 줄은 오탐 안 됨(단위검증 완료).
- 색 토큰: `primary-soft`=#F1E4E4, `on-primary-soft`=#661F20(칩). design-tone 메모리(소프트·차분) 준수 — 강렬 마룬 회피.
- 검증: 타입체크·프로덕션 빌드·실제 스크린샷 텍스트 파싱/재정렬/오탐 단위검증 전부 통과.

## 다음 세션 백로그 (v2bb 이월 + 신규)

1. **(신규, 옵션)** 인사이트 카드 칩 색·간격·소제목 강조 등 **실기기 시각 미세조정** — 우진 피드백 따라.
2. **(v2ba 이월)** 스케줄 `honduras-news-0600` 첫 "Run now" — WebSearch/Bash/Write 권한 task 저장 확인(우진, 사이드바 Scheduled).
3. (보류 합의) **C3 baseline SQL** — 실DB 스키마 덤프 → `supabase/baseline-*.sql` 1개(우진 콘솔 협조, 별도 세션). 현재 patch61~90 순차 실행 의존.
4. (보류 합의) **C4 postcss moderate 2건** — 빌드타임 devDep, 실위험 낮음. `audit fix --force`는 Next 버전 리스크 → 건드릴 때 빌드 검증 필수.
5. Next 16 (보류) / (옵션) archive 페이지네이션·뉴스 출처 링크화 (v2ba 이월).

## 중단 결정 (재제안 금지 — 메모리 `no-crud-abstraction`)

- DeleteButton·List·BulkPanel 3종 공통화 중단(우진 확정, v2bb 실측 근거).

*작성: 2026-06-10 세션 종료. 인사이트 표시 포매터 3 commit(f0610c0·8a7310c·04c08ba) push 완료. 타입체크·빌드·단위검증 전부 통과. 자동 인사이트 루틴(insight-update 등)은 변경된 프롬프트(Prayer·Letter 기도제목 순서)를 다음 생성부터 사용.*
